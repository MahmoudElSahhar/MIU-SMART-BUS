<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.mahmo.loginform">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="com.google.android.providers.gsf.permission.Read_GSERVICES" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <uses-feature
        android:glEsVersion="0x00020000"
        android:required="true" />

    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality. 
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/newicon"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".HomeScreenActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".RegistrationActivity" />
        <activity android:name=".StudentSignInActivity" />
        <activity android:name=".StudentWelcomeActivity" />
        <activity android:name=".StudentToUniActivity" />
        <activity android:name=".StudentToHomeActivity" />
        <activity android:name=".BusWelcomeActivity" />
        <activity android:name=".BusLoginActivity" />
        <activity android:name=".AdminLoginActivity" />
        <activity android:name=".AddingBusLinesActivity" />
        <activity android:name=".AdminWelcomeActivity" />
        <activity android:name=".RemovingBusLineActivity" />
        <activity android:name=".RemoveStudentActivity" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/. 
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name=".AdminWatchLiveActivity"
            android:label="@string/title_activity_watch_live" />
        <activity
            android:name=".StudentWatchLiveActivity"
            android:label="@string/title_activity_student_watch_live" />
        <activity
            android:name=".WatchBusStopsOnMapActivity"
            android:label="@string/title_activity_watch_bus_stops_on_map" />
        <activity
            android:name=".NearestStopOnMapActivity"
            android:label="@string/title_activity_nearest_stop_on_map" />
        <activity
            android:name=".FindDestinationOnMapActivity"
            android:label="@string/title_activity_find_destination_on_map" />
        <activity
            android:name=".StopLocationOnMapActivity"
            android:label="@string/title_activity_stop_location_on_map" />
        <activity
            android:name=".BusOnMapActivity"
            android:label="@string/title_activity_bus_on_map" />
        <activity android:name=".BusesInfoActivity" />
        <activity android:name=".AddBusesActivity" />
        <activity android:name=".RemoveOrEditBusActivity" />
        <activity android:name=".ScheduleActivity" />
        <activity android:name=".ScheduleSecondActivity" />
        <activity android:name=".QRScannerActivity" />
        <activity android:name=".QRGeneratorActivity" />
        <activity android:name=".AddIdImageActivity" />
        <activity android:name=".GetStudentIDActivity" />
        <activity android:name=".BusRatingActivity" />
        <activity android:name=".GetBusRatingActivity"></activity>
    </application>

</manifest>